package elevator.scheduling;

/**
 * A predictably unsophisticated scheduler that isn't completely awful.
 */
public class RRFIFOScheduler implements Scheduler {
    // TODO constructor with elevators as param
}
